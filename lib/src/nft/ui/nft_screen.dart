import 'package:flutter/material.dart';

class NftScreen extends StatelessWidget {
  const NftScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('NftScreen '),
      ),
    );
  }
}

